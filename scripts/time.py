# write code to diff the time between two dates
# and print the result in days, hours, minutes, seconds
# use the datetime module
# use the timedelta object
# use the strptime function to convert a string to a datetime object
# use the strftime function to convert a datetime object to a string
# use the now function to get the current date and time
# use the replace function to change a datetime object
# use the total_seconds function to get the total seconds from a timedelta object
import datetime
asdf = datetime.datetime.now()
print(asdf)
print("The variable asdf has the value of " + asdf)
print("The variable asdf has the value of " + str(asdf))
